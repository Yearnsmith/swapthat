<h2 class="title is-2"><%=@user.username%></h2>
<div class="card">
    <div class="card-content">  
    <p class="content">&hellip;there's nothing much to see yet&hellip;</p>
    <%= link_to "See #{@user.username}'s listings", user_listings_url(@user.username)%>
    </div>